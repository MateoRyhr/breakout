import * as data from './config/dataManager.js'
import * as objects from './engine/objects.js'
import * as render from './engine/render.js'
import * as controls from './engine/controls.js'
import * as engine from './engine/main.js'
import * as maps from './maps/maps.js'
import * as ui from './ui/ui.js'
import * as audio from './audio/audio.js'

// function main() {
//   engine.moving(config)
//   engine.detectCollision(config)
//   render.draw(config)
//   engine.win(config,audio)
//   // if (win()) {
//   //   level++;
//   //   bienNeneSound();
//   //   initObjects(level)
//   //   if (level == 3) {
//   //     if (!winner) {
//   //       setTimeout(alert("Ganaste"), 100);
//   //       setTimeout(clearInterval(running), 100);
//   //       winner = true;
//   //     }
//   //   }
//   // }
//   lose();
// }

// ui.setupUi(config.ui,main,config.speed)
// ui.controlsConfig(config.ui)